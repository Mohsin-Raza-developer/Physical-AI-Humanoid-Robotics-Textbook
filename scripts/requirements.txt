# Embeddings Generation Dependencies
# Python 3.10+ required

# Core AI/ML Libraries
cohere>=5.0.0,<6.0.0                    # Cohere embed-v4.0 API for generating embeddings
qdrant-client>=1.7.0,<2.0.0             # Qdrant vector database client

# Text Processing & Chunking
langchain>=0.1.0,<0.2.0                 # LangChain framework for text splitting
langchain-text-splitters>=0.0.1         # Text splitting utilities
tiktoken>=0.5.0,<1.0.0                  # OpenAI tokenizer for accurate token counting

# Markdown & Configuration
python-frontmatter>=1.1.0,<2.0.0        # YAML frontmatter parsing for Docusaurus files
python-dotenv>=1.0.0,<2.0.0             # Environment variable management

# Progress & Utilities
tqdm>=4.66.0,<5.0.0                     # Progress bars for file processing
tenacity>=8.2.0,<9.0.0                  # Retry logic with exponential backoff

# Development Tools (Optional)
mypy>=1.8.0                             # Static type checking
types-tqdm                               # Type stubs for tqdm
